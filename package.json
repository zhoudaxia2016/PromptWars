{
  "name": "promptwars",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "pnpm --filter @promptwars/web dev",
    "build": "pnpm --filter @promptwars/cubic build && pnpm --filter @promptwars/web build",
    "preview": "pnpm --filter @promptwars/web preview"
  },
  "devDependencies": {
    "@types/deno": "^2.5.0",
    "typescript": "^5.6.0"
  }
}
